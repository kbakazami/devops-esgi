---
- name: "Start ansible"
  hosts: localhost

  tasks:
    - name: Create mysql
      state: started
      community:
        general:
          docker_image:
            name: Mysql
            build:
              path: ./docker/mysql